import type { FC } from "react"

interface NoteListToolbarMobileProps {
  search: { filter: string }
}

export const NoteListToolbarMobile: FC<NoteListToolbarMobileProps> = () => {
  return <div />
}

export default NoteListToolbarMobile
